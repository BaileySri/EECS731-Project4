from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict the scores for each team using other columns with regression models',
    author='Bailey Srimoungchanh',
    license='',
)
